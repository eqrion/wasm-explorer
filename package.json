{
  "type": "module",
  "scripts": {
    "build": "npm run typecheck && npm run build-js && npm run build-css && npm run copy-files",
    "build-js": "for source in Worker.ts App.tsx; do esbuild --bundle --format=esm src/$source --outdir=dist; done",
    "build-css": "npx @tailwindcss/cli -i ./pages/style.css -o ./dist/style.css",
    "copy-files": "cp pages/index.html pages/favicon.png pages/logo.svg dist",
    "clean": "rm -rf dist",
    "typecheck": "tsc --jsx react",
    "watch": "nodemon --watch 'pages/**/*' --watch 'src/**/*' --ext 'html,css,ts,tsx' --exec 'npm run build'",
    "format": "prettier --write 'src/**/*.{ts,tsx}' 'pages/**/*.{html,css}'",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:unit:coverage": "vitest run --coverage",
    "test:e2e": "npm run build && playwright test",
    "test:e2e:ui": "npm run build && playwright test --ui",
    "test": "npm run test:unit && npm run test:e2e",
    "build-extension": "cd vscode-extension && node build.mjs",
    "package-extension": "cd vscode-extension && npx @vscode/vsce package"
  },
  "dependencies": {
    "@bytecodealliance/preview2-shim": "^0.17.8",
    "@tailwindcss/cli": "^4.1.11",
    "react": "^19.1.0",
    "react-dom": "^19.1.1",
    "tailwindcss": "^4.1.11",
    "typescript": "^5.4.5",
    "waside": "0.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.0.0",
    "@testing-library/user-event": "^14.5.2",
    "@types/react": "^19.1.9",
    "@types/react-dom": "^19.1.7",
    "@vitest/coverage-v8": "^4.1.4",
    "esbuild": "0.25.5",
    "jsdom": "^25.0.0",
    "nodemon": "3.1.10",
    "prettier": "3.6.2",
    "serve": "^14.2.3",
    "vitest": "^4.1.4"
  }
}
